server:
  port: 3066

spring:
  application:
    name: trade-user-service

  datasource:
    url: jdbc:mysql://localhost:3307/tradding_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true